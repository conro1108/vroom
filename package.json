{
  "name": "vroom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pixel-art, mobile-first arcade racing game.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
